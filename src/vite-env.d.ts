/// <reference types="vite/client" />

// Injected by vite.config.ts's `define` from package.json's
// createGristWidgetVersion field -- null for the pristine template source
// itself (that field only exists on scaffolded copies).
declare const __CREATE_GRIST_WIDGET_VERSION__: string | null
